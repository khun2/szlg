#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, asd;
    cin >> n;

    map<int, vector<int>> m;
    for(int i = 1; i < n +1; i++) {
        cin >> asd
        m[i].push_back(asd);
    }
}