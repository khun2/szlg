#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, x, l = 0, r = 1;
    cin >> n >> x;
    vector<int> v(n), pref(n+1);
    for(int i = 0; i < n; i++) {
        cin >> v[i];
    }
    pref[0] = 0;
    for(int i = 1; i <= n; i++) {
        
    }
    while(r <= n) {
    
    }
}